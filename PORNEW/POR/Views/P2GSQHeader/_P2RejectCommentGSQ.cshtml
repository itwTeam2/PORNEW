@model IEnumerable<POR.Models.LivingInOut._GSQHeader>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    foreach (var item in Model)
    {
        <fieldset id="reject">
            <div class="form-horizontal">
                <div class="form-group col-md-12">
                    @Html.TextAreaFor(model => item.Comment, new { @class = "form-control", autocomplete = "off", id = "txtComment" })
                    @Html.ValidationMessageFor(model => item.Comment, string.Empty, new { @class = "label label-danger" })

                </div>

                <div class="form-group">
                    <div class="col-md-10">
                        <input type="button" value="Save Comments" class="btn btn-primary" id="btnSubmitReject" />
                        @*@Html.ActionLink("Save Comments", "Index_Reject", "Married", new { CSHID = item.CSHID , FMSID = item.FMSID, Comment = item.Comment}, new { @class = "btn btn-info" })*@
                    </div>
                </div>
            </div>
            <input type="hidden" id="hndGSQHID" value="@Html.DisplayFor(model => item.GSQHID)" />
            <input type="hidden" id="hndFMSID" value="@Html.DisplayFor(model => item.FMSID)" />
        </fieldset>
    }
}
<script type="text/javascript">
    $("#btnSubmitReject").click(function () {
        var Comment = $("#txtComment").val();
        var GSQHID = $("#hndGSQHID").val();
        var FMID = $("#hndFMSID").val();
        debugger;
        $.ajax({
            type: "POST",
            url: "./Index_Reject",
            //url: "/POR/Married/Index_Reject", /// URL in Server side
            //url: "/Married/Index_Reject", /// URL in Local Server
            dataType : 'json',
            data: { id: Comment, GSQHID: GSQHID, FMSID: FMID },
            success: function (result) {
                alert(result.Message);
                var url = '@Url.Action("RejectIndex", "P2GSQHeader")';
                window.location.href = url;
            }
        })
    })
</script>
@Scripts.Render("~/bundles/jquery")
